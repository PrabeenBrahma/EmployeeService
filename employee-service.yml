spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/prabeen_repo
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update   # 👈 fixed typo (was "updateeureka")

eureka:
  client:
    register-with-eureka: true    # 👈 fixed key (was "registry-with-eureka")
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761
